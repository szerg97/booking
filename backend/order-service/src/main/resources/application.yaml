server:
  port: 8080

nats:
  topic:
    name: "hello"

management:
  endpoints:
    web:
      exposure:
        include: [ "health", "prometheus", "metrics" ]
      base-path: '/actuator'
  endpoint:
    health:
      show-details: always
    metrics:
      enabled: true
    prometheus:
      enabled: true
